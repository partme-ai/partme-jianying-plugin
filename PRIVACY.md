# Privacy

JianYing Edit is a local plugin. No telemetry, no hosted data service.

The engine reads: the JianYing draft root (detection only), media files referenced by the edit plan, and writes new draft folders under the draft root. It never modifies or deletes existing drafts. All generation is local; nothing is transmitted to any network service.
